@model IEnumerable<Copa_do_Mundo.Models.AreaModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ValorIngresso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ValorIngresso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDArea">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDArea">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDArea">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
